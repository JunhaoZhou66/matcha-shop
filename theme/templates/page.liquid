{% comment %}
  Default Page Template
{% endcomment %}

<div style="padding: 2rem 3rem; max-width: 800px; margin: 0 auto;">
  <h1 class="serif" style="font-size: 2.5rem; margin-bottom: 2rem;">{{ page.title }}</h1>
  <div class="page-content">
    {{ page.content }}
  </div>
</div>
